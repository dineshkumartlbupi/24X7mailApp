import 'package:flutter/material.dart';

class UnreadMailView extends StatelessWidget {
  const UnreadMailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
